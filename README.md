# Projet SM501 – Algorithme de Floyd–Warshall

Ce dépôt contient :

- Le code source complet (`src/`)
- Les fichiers de tests (`tests/`)
- Les traces d'exécution pour chaque graphe (`traces/`)
- Le rapport PDF du projet (`doc/Projet_Graphes.pdf`)
- Le Makefile permettant de compiler le programme

## Compilation

## Exécution

## Organisation du dépôt
- `src/` : fichiers .c et .h
- `tests/` : fichiers de graphes au format .txt
- `traces/` : traces d’exécution 
- `doc/` : documents (rapport PDF)

Projet réalisé par Cavaignac Romain, Dubernet Mathieu, Haegeman Victor, Prosper Léo – ING1NEW(GRP3) – EFREI.
